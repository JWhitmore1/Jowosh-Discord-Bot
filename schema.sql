CREATE TABLE IF NOT EXISTS pairs (
    ID TEXT PRIMARY KEY NOT NULL,
    hugs INT DEFAULT 0,
    kills INT DEFAULT 0,
    killeds INT DEFAULT 0,
    kisses INT DEFAULT 0,
    slaps INT DEFAULT 0,
    bites INT DEFAULT 0,
    pats INT DEFAULT 0
);